# 2026-09-22 工作日志

## 涨停复盘（自动化触发，15:30）

- 走 `a-share-limit-up-review` 四步流程：`zt_fetch.py 20260922 20260921` → `zt_analyze.py` →
  `scripts/zt_report_20260922.py`（新建）→ `_check_js.py` / `_shot.py` 自检。
- 产出 `reports/涨停复盘对比-20260922.html`（31KB，7 图全部 canvas=1，占位符残留 0，
  `node --check` OK，0 控制台错误，无非法时间，无 >100% 百分数）。
- 核心结论：**放量分歧日 / 宽度腰斩、高度续命**。涨停 101→62（-39），封板率 80.2%→77.5%，
  跌停 2→3，而两市成交额 20315→21356 亿（+1040 亿 / +5.1%，本轮修复以来最高）。
  全部指数**高开低走**（科创50 高开 +2.51% 收 +0.46%，距当日最高 -2.50%）；
  增量集中在权重（科创50 成交 +27.2%、上证50 +17.8%），小微盘转平（国证2000 0.00%、中证1000 -0.09%）。
  → 结论：增量是配置盘，打板资金在退。
- 结构特征：首板 80→38 腰斩，2 板以上 21→24 微增，最高板 5→6（华瓷股份）；
  晋级率 23.8%（连板组 33.3%/中位 +2.77%、首板组 21.2%/中位 +1.05%、31 只翻绿）。
  行业上医药链集体退潮（化学制药 8→3、医疗服务 5→2、医疗器械 4→1），
  钱流向出版 3→4、家居用品 3→5、广告营销 1→3、元件 2→3 —— 其中家居/元件的多只是**跨界半导体**
  （梦天家居、爱丽家居、大亚圣象、爱普股份、时空科技），本质是壳资源 + 资产注入预期。
- 当日最独特发现：**12 只含「华」字标的涨停，且 100% 位于 2 板及以上**，占全市场 2 板以上梯队的 50%；
  其中出版/文化的「新华系」3 家（内蒙新华 5 板、新华文轩 3 板、新华传媒 2 板），
  叙事为文化资产整合 → 主线真空期的纯名称博弈。报告中单列成表并标注脆弱性。
- 口径自检：东财涨停池 tc=63 vs 同花顺 62，差额 1 只为北交所 920526（断言通过）；
  跌停家数两源汇总一致 3 家，但同花顺跌停池明细接口当日 **404**（`ths_dt`=None），故未列名单。

## 新增工程经验（已回写 skill）

1. **东财炸板池 `em_ZB` 含「盘中打开后回封」的票**（09-22 实测 18 只中瑞芯微收 +10.00%），
   不能整池当「炸板未回封」讲 —— 须按收盘涨幅是否达涨停（创业板/科创板阈值 19.8%）拆分。
   → `pitfalls.md` 第 12 条 + SKILL.md 自检项。
2. **`idx_cmp` 只覆盖 4 个指数**（上证/深成/创业板/科创50），上证50、中证1000、国证2000 的
   「昨日涨跌幅」与「成交额环比」需从上一交易日 `out/zt_review_{D1}.json` 的 `indexes` 补全，
   否则表格渲染成 `—`（不报错但读者会以为缺数据）。这是判断「增量去权重还是小微盘」的必要数据。
   → `pitfalls.md` 第 13 条 + SKILL.md 自检项。
3. SKILL.md 新增 **回归基线 5**（09-22 放量分歧日样本）+ 两条特征值读法。

## 报告脚本新增占位符（供下次复用）

- `HUA_N` / `HUA_PCT` / `HUA_LB_N` / `HUA_LB_PCT` / `XH_N` / `XH_LIST`：按**名称**统计的「某字辈」族群。
- `ZB_POOL_N` / `ZB_SEALED_N` / `ZB_SEALED_NAME` / `ZB_SEALED_V` / `ZB_TOP1..3(_V)`：炸板池回封拆分。
- `*_GAP` / `KC_DRAW`：各指数开盘缺口与距当日最高回落（高开低走量化）。
- `*_AMT_PCT`（如 `SZ50_AMT_PCT`）：各指数成交额环比，用于风格量能对比。
- `AMT_MED` / `AMT_MED_Y`：涨停股单只中位成交额（占比跌破 3% 时的三角校验）。
