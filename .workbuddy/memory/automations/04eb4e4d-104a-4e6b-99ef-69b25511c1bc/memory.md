# 自动化执行记录：A股涨停复盘（每日盘后）

任务 ID：04eb4e4d-104a-4e6b-99ef-69b25511c1bc
内容：① 复盘当日涨停股并与前一交易日对比、分析资金运动 ② 将 repo 提交到远程

## 执行历史

### 2026-09-17（D0=20260917 vs D1=20260916）
- 走 `a-share-limit-up-review` skill 四步流程：zt_fetch → zt_analyze → zt_report_20260917.py → _check_js/_shot 自检。
- 产出 `reports/涨停复盘对比-20260917.html`（27KB，7 图，占位符残留 0，JS OK，无控制台错误）。
- 核心结论：宽度收缩、高度保留的结构切换日（涨停 47/昨 89、封板率 70.1%、跌停仅 1 家、量能持平 -0.9%）；
  算力链止盈 → 汽车链接棒（通信设备 6→1、汽车零部 3→8）。
- 修了一个 skill 模板 bug 并已回写：`sorted(key=-(x["pct"] or -99))` 会把 0.00% 当缺失值 →
  「最弱」KPI 显示成平盘股。已改为显式 None 判断，并把该坑写进 `references/pitfalls.md`（口径类第 6 条），
  同时在 pitfalls 与 SKILL.md 补充说明「3%~8% 占比自检」与家数强相关、需用单只中位成交额二次校验。
- git：3 个 commit 已 push 到 origin/main（ccb6086..0852c42）。

### 2026-09-19（周六补跑，D0=20260918 vs D1=20260917）
- 当日为周六非交易日，按「最近一个交易日」取 D0=20260918（周五）、D1=20260917、D2=20260916。
- 走完整四步流程，产出 `reports/涨停复盘对比-20260918.html`（30KB，7 图，占位符残留 0，JS OK，无控制台错误）。
- 核心结论：**放量普涨扩张日**（涨停 47→77、封板率 75.5%、跌停归零、两市成交 +13.9%、晋级率 25.5%）；
  汽车链一日游（汽车零部 8→2），资金改道电网设备 1→6 / 半导体 2→5 / 消费地产低位补涨；
  首次出现权重涨停（华天科技、万科A、绿地控股）；短板是最高板 5→4、首板占 84%。
- 修了第二个 skill bug 并已回写：**东财 `em_ZB.pool` 的 `fbt` 是 HHMMSS 整数，切分前必须 `zfill(6)`**，
  旧写法把 10:00 前的封板时间渲染成 `92:50`。已修脚本 + 重新生成 0917/0918 两份报告，
  `hhmm()` 写进 skill 模板，坑进 `references/pitfalls.md` 口径类第 7 条，SKILL.md 自检项与 `回归基线 3` 同步更新。
- 口径发现：东财涨停池含北交所（09-18 东财 78 vs 同花顺 77，差额 920298），报告须显式声明沪深口径。
- git：1 个 commit `6809c2c` 已 push 到 origin/main（0852c42..6809c2c）。

### 2026-09-20（周日补跑 → 周度复盘，D=20260911~20260918）
- 当日周日休市、09-18 已于 09-19 复盘完毕 → **改为周度复盘**，避免重复出逐日报告。
- 新增 `scripts/zt_week_analyze.py` + `scripts/zt_week_report.py`，产出
  `reports/涨停周报-20260911至20260918.html`（27KB，7 图，占位符残留 0，JS OK，0 控制台错误）。
- 补跑 `zt_fetch.py 20260914 20260911` 补齐周一数据；周报输入为**多份** `out/zt_review_*.json` 自动合并。
- 关键方法：**晋级率用「两日涨停池代码集合取交集」**，不依赖行情快照 → 可跨历史日期批量复算，
  与逐日报告口径核对一致（9/16=37.5%、9/17=10.1%、9/18=25.5%）。
- 核心结论：一周五段「缩量分歧(9/14) → 地量杀跌(9/15) → V型反转(9/16) → 收敛去伪(9/17) → 放量二波(9/18)」；
  高度递减 4→5→6→5→4（宽度驱动）；上游元件 9→1 流出，钱去下游算力应用 + 半导体 + 电网设备 + 低位消费。
- **新增工程约束：ECharts 改本地托管**（`assets/echarts.min.js` + CDN 兜底），
  原因是 jsdelivr 在 Playwright 侧间歇性 `ERR_CONNECTION_RESET` → 自检报 `canvas=0` + `echarts is not defined`，
  而 `curl` 同时刻能通，极易误判为图表配置错误。skill 模板与 pitfalls 已同步。
- 另修：报告避坑「ECharts 类目轴首项在底部」→ 排名类热力图数组须反转。
- 也自查出并修正了 4 处硬编码数字/错误日期标签（把 9/15 标成周一、缺失溢价日期写错等）。
- git：1 个 commit `78c5194` 已 push 到 origin/main（6809c2c..78c5194）。

### 2026-09-21（周一，D0=20260921 vs D1=20260918）
- 走完整四步流程，产出 `reports/涨停复盘对比-20260921.html`（26KB，7 图全部 canvas=1，占位符残留 0，
  `node --check` OK，0 控制台错误，无非法时间）。
- 核心结论：**缩量扩散日**。涨停 77→101（+24）、封板率 75.5%→80.2%、跌停 0→2，而两市成交额 20771→20315 亿（-2.2%）
  → 家数扩张来自**存量资金向低位扩散**，不是增量入场（这是本报告的主论点）。
  风格由大盘成长切向小微盘（国证2000 +1.38% / 中证1000 +1.02% vs 科创50 +0.29%）。
  行业迁移是**跨赛道搬家**：电网设备 6→1、半导体 5→2 几乎清空，医药链集体爆发（化学制药 0→8、医疗服务 2→5、
  医疗器械 0→4，合计 24 家占 23.8%）+ 地产链补涨。晋级率 27.3%（连板组 66.7%/中位 +9.96%、首板组 20.0%/中位 +1.40%）。
  封单合计 81.4 亿但新华传媒一只占 24.89 亿，**中位数反而从 0.52 亿降到 0.42 亿**。
- **口径发现 1**：东财 `em_ZT.tc`=103 vs 同花顺 101，差额 2 只均为北交所（920478 峆一药业、920427 华维设计）。
  报告口径说明已显式列出并声明「按沪深口径取 101」。
- **口径发现 2**：同花顺跌停池明细接口 `dataapi/limit_up/limit_down_pool` 当日 **404**（`ths_dt`=None），
  东财 `em_DT` 出现 `tc=2 / pool=[]` 的自相矛盾态 → 跌停家数只能用汇总字段，报告已声明明细不可用。
- **排查记录（非 bug）**：`zt_analyze.py 20260921 20260918 20260917` 的【一】段把「前日 20260917」打成全 0，
  误以为 D2 数据缺失。实为设计如此——`zt_fetch.resolve_dates` 只取前 2 个参数，review 文件只含 D0/D1；
  但报告的三日序列用的是 `senti[D1]["zt_prev"]`（D1 记录自带的「前一交易日」汇总值），**结果正确，不必补抓 D2**。
  这一点已写进 skill 与项目 MEMORY.md，避免下次重复排查。
- skill 已回写（本次改为**整个 skill 目录进版本控制**，之前 `.workbuddy/` 全忽略）：
  SKILL.md 新增「北交所差额自检」「跌停池 404 处置」两条必做自检 + **回归基线 4**（09-21 样本）；
  `references/pitfalls.md` 新增口径类第 8 条（北交所差额归因 + `assert all(c.startswith("92"))` 代码片段）
  与第 9 条（跌停池 404）。
- MEMORY.md 因超注入上限（13.7KB 被截断）做了一次全量合并重写（→ 约 9KB），保留全部可复用结论。
- git：1 个 commit `f6d1323` 已 push 到 origin/main（78c5194..f6d1323）。

### 2026-09-21（同日第二次触发 16:10，复跑校验 → 复用既有交付，无新报告）

- **本次先查状态再动手**：发现同日 15:38 已产出报告、commit `f6d1323` 已 push → 不重做，改为**幂等验证**。
- 重跑 `zt_fetch.py 20260921 20260918` 后按 `code` 排序归一化比对两份 JSON：
  家数（101/77）、涨跌幅、封板时间、成交额**全不变**；重跑 analyze + 报告脚本后新 HTML 与已提交版本
  **逐字节一致（`git diff --stat` = 1 insertion / 1 deletion，仅生成时间行）** → 复用，未产生新报告。
- **新口径发现（已回写 skill）**：同花顺封单 `order_amount` / `order_volume` 在**收盘后 30 分钟内仍在刷新**。
  15:31 vs 16:11 两次抓取，4 只差 18~54 倍（我爱我家 327万→5916万、百通能源 220万→1.198亿、
  华森制药 106万→3550万、奥赛康 13.3万→271万）。报告封单取**东财 `fund`**，故结论未受影响；
  但若改口径用同花顺封单，**必须 16:00 后再抓**。→ `pitfalls.md` 口径类第 10 条。
- **比对方法教训**：同一份数据两次抓取的**列表顺序本身不稳定**（`em_ZB` / `ths_block` 首项互换），
  直接 diff 会看到几十处假差异，必须先按 `code` 排序归一化。代码片段已进 pitfalls 第 10 条。
- git：1 个 commit `12d4ebf` 已 push 到 origin/main（f6d1323..12d4ebf），内容为 skill 文档 + 当日日志。
- 工作区收尾：重新生成的 HTML 已还原为已提交版本，临时备份已删，工作区干净（仅剩已知的 `scratch_fund.txt`）。

### 2026-09-21（同日第三次触发 16:33，复跑校验 → 复用既有交付，无新报告）

- **先查后做**：报告（15:38 生成）与 commit `f6d1323` 均在 → 走幂等验证，不重做。
- 连抓 3 次（16:11 备份 / 16:34 / 16:36），**四个核心键 `D0`/`D1`/`today_zt_quotes`/`yesterday_zt_today` 全部一致**
  → 报告可复用，未生成新报告。
- **新坑（已回写 skill pitfalls 第 11 条）**：`index_hist` 指数日线**盘后会把「当日行」移出返回窗口**。
  16:11 抓取末行含 `20260921`，16:34 与 16:36 两次抓取末行均为 `20260918`（当日消失后稳定不复现）—— 接口侧行为。
  **不影响报告**：当日成交额取自实时快照 `indexes[sym].amount_wan`，历史行按显式日期查 D1/D2，对窗口滑动免疫。
  但**禁止**用 `index_hist[code].rows[-1]` 当「当日」（会静默回退一天且不报错）。
- **幂等比对规则升级**：只比四个核心键；必须排除 `generated_at` / `dates` / `indexes`（市值尾数抖动）/
  `index_hist`（本条窗口滑动）—— 否则会看到 8 行假差异而误判为数据变化、白跑完整流程。
- 收尾：`out/zt_review_20260921.json` 从 16:11 备份还原（含当日日线，与已提交报告同源），临时文件已清。
- git：1 个 commit `537500f` 已 push 到 origin/main（12d4ebf..537500f），内容为 skill 文档 + 当日日志 + 项目 MEMORY.md。
- 在线链接已 curl 验证：HTTP 200，页面 title = 「涨停复盘 · 2026-09-21（对比 9-18）」。

### 2026-09-22（周二，D0=20260922 vs D1=20260921）
- 交易日确认（周二、15:30 触发），今日报告不存在 → 走完整四步流程。
  产出 `reports/涨停复盘对比-20260922.html`（31KB，7 图全部 canvas=1，占位符残留 0，`node --check` OK，
  0 控制台错误，无非法时间/超范围百分数，北交所差额断言通过）。
- 核心结论：**放量分歧日 / 宽度腰斩、高度续命**。涨停 101→62、封板率 80.2%→77.5%、跌停 2→3，
  而两市成交额 20315→21356 亿（+1040 亿 / +5.1%，本轮修复以来最高）。全部指数**高开低走**
  （科创50 高开 +2.51% 收 +0.46%，距当日最高 -2.50%）；增量集中在权重（科创50 成交 +27.2%、上证50 +17.8%），
  小微盘转平 → 增量是配置盘、打板资金在退。首板 80→38 腰斩，2 板以上 21→24，最高板 5→6（华瓷股份）。
  医药链集体退潮（化学制药 8→3），钱流向出版 3→4 / 家居用品 3→5 / 广告营销 1→3 / 元件 2→3，
  其中家居与元件多只是**跨界半导体**（壳资源 + 资产注入预期）。
- **当日最独特发现**：12 只含「华」字标的涨停且 **100% ≥2 板**，占 2 板以上梯队 50%；
  「新华系」出版 3 家叙事为文化资产整合 → 主线真空期的纯名称博弈，报告中单列成表并标注脆弱性。
- 口径自检：东财 tc=63 vs 同花顺 62，差额 1 只 = 北交所 920526；跌停家数两源汇总一致 3 家，
  但同花顺跌停池明细接口当日 **404**（`ths_dt`=None），报告声明「仅计家数、未列名单」。
- **两条新工程经验已回写 skill**：① 东财炸板池 `em_ZB` 含「盘中打开后回封」的票（09-22 实测 18 只中
  瑞芯微收 +10.00%），须按收盘涨幅是否达涨停（创业板/科创板 19.8% 阈值）拆分，不可整池当"炸板未回封"；
  ② `idx_cmp` 只覆盖 4 个指数，上证50/中证1000/国证2000 的昨日涨跌幅与成交额环比须从
  上一交易日 `out/zt_review_{D1}.json` 的 `indexes` 补全，否则表格渲染成 `—`。已进 `pitfalls.md` 12/13 条
  + SKILL.md 必做自检，并新增**回归基线 5**。
- **新增报告脚本占位符**（下次可直接复用）：`HUA_*` / `XH_*`（名称族群统计）、
  `ZB_POOL_N` / `ZB_SEALED_N` / `ZB_TOP*`（炸板池回封拆分）、`*_GAP` / `KC_DRAW`（高开低走量化）、
  `*_AMT_PCT`（各指数成交额环比）、`AMT_MED(_Y)`（单只中位成交额三角校验）。
- git：1 个 commit `f1b7512` 已 push 到 origin/main（537500f..f1b7512），内容为报告 + 报告脚本 + skill 文档 + 当日日志 + MEMORY.md。
  在线链接已 curl 验证：HTTP 200，title = 「涨停复盘 · 2026-09-22（对比 9-21）」，7 个图表容器。
- 注：`git pull --rebase` 要求工作区干净，故本次顺序为 **先 commit → 再 pull --rebase → 再 push**（直接先 pull 会报
  "cannot pull with rebase: Your index contains uncommitted changes"）。

## 后续注意（累积，最新）
- **幂等比对只比 `D0` / `D1` / `today_zt_quotes` / `yesterday_zt_today` 四个核心键**，
  其余键（`generated_at` / `dates` / `indexes` / `index_hist`）有接口侧抖动，纳入比对必然误判（见 pitfalls 第 11 条）。
- 该自动化在**同一交易日可能被触发多次**（09-21 触发 3 次：15:38 出报告、16:10 与 16:33 幂等验证）。
  每次都按「先查 → 再验证 → 不重做」走，避免产出重复报告与冗余提交。
- `.workbuddy/` 内文件（含 skill / memory）虽在 `.gitignore` 中，但**已跟踪**；
  `git add` 会被 ignore 规则拒绝，**必须 `git add -f`**（09-21 实测：不加 -f 时 add 静默失败、commit 未产生）。
- 次日运行前无需再改 skill 脚本；`zt_report_{D0}.py` 需从模板复制后重写叙述文案（唯一不可自动化的一步）。
- 模板已内置 `hhmm()`；写炸板池表格时直接调用，不要再手写 `str(int(fbt))[:2]`。
- `r0`（同花顺源）的 `fbt` 是 `"09:30:09"` 字符串，与东财池的整数型 `fbt` **同名不同型**，不要混用同一段格式化代码。
- 每次复盘后都扫一遍「报告里有没有物理上不可能的值」（如 92:50、占比>100%）。
- **每次出报告前先跑一次北交所差额断言**（`set(em) - set(ths)` 必须全是 `92` 开头）。
- **跌停家数永远取汇总字段**，别数池子；明细接口 404 是常态，报告里如实声明即可。
- **同一交易日重复触发时先验证、不重做**：`git log` + `ls reports/` 看当日报告是否已存在 →
  重抓一次做「按 `code` 排序归一化」比对 → 仅生成时间不同即复用，不产生冗余报告与提交。
- **收盘 ≠ 数据稳定**：同花顺封单字段 15:30~16:10 仍在变（见 pitfalls 第 10 条）。
  盘后立即跑完的复盘，涉及封单的结论记得复核；跨源比对前先确认各自是否已结算。
- `.workbuddy/` 仍在 .gitignore 中；skill / memory 文件需 `git add -f` 才会被提交（现已纳入跟踪）。
- 报告只在 `reports/` 与 `scripts/` 落盘可提交；`out/` 已忽略。
- 仓库遗留未跟踪垃圾文件 `scratch_fund.txt`（gb2312 404 页面），未提交。
