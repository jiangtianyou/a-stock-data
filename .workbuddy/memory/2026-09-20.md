# 2026-09-20（周日，休市）

## A股涨停复盘自动化（任务 04eb4e4d）

今日为周日，无新交易时段；最近交易日 2026-09-18 已于 09-19 复盘完毕。为让周末的自动化仍产出增量价值，
改为**周度复盘**：新增 `scripts/zt_week_analyze.py` + `scripts/zt_week_report.py`，产出
`reports/涨停周报-20260911至20260918.html`（27KB，7 图，占位符残留 0，JS OK，无控制台错误）。

- 为补齐 09-14（周一）数据，补跑 `zt_fetch.py 20260914 20260911`（落 `out/zt_review_20260914.json`）。
- 周报输入是**多份** `out/zt_review_*.json`，按日期自动合并；晋级率改用「前一日涨停池代码集合 ∩ 当日涨停池代码集合」，
  不依赖行情快照，实测与逐日报告口径完全一致（9/16=37.5%、9/17=10.1%、9/18=25.5%）。
- 本周核心结论：五段「缩量分歧 → 地量杀跌 → V 型反转 → 收敛去伪 → 放量二波」；
  周二地量 16,127 亿是唯一卖压衰竭信号，周五跌停归零 + 放量 +13.9% + 早盘封板前移；
  高度递减（最高板 4→5→6→5→4），属宽度驱动。资金搬家：上游元件（行业口径 9→1）流出 → 下游算力应用 + 半导体 + 电网设备 + 低位消费。
- **ECharts 改为本地托管**：`assets/echarts.min.js`（1.0MB，jsdelivr 下载），页面写「本地优先 + CDN 兜底」，
  解决 Playwright 渲染自检时 CDN 频繁 `ERR_CONNECTION_RESET` 导致 canvas=0 的问题。
- 修正了 4 处「硬编码数字 / 日期标签写错」（把 9/15 写成周一、把缺失溢价的日期写错等），全部改为占位符注入。

## git

commit + push 到 origin/main（详见提交记录）。
