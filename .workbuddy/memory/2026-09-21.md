# 2026-09-21（周一）工作日志

## 涨停复盘（自动化任务 04eb4e4d）

- 流程：`zt_fetch.py 20260921 20260918` → `zt_analyze.py 20260921 20260918 20260917` →
  `scripts/zt_report_20260921.py` → `_check_js.py` / `_shot.py` 自检。
- 产出：`reports/涨停复盘对比-20260921.html`（26KB，7 图全部 canvas=1，0 控制台错误，占位符残留 0，无非法时间）。
- 核心结论：**缩量扩散日** —— 涨停 77→101（+24）、封板率 75.5%→80.2%、跌停 0→2，但两市成交额 20771→20315 亿（-2.2%），
  即家数扩张来自存量资金向低位扩散而非增量。风格从大盘成长切向小微盘（国证2000 +1.38% / 中证1000 +1.02% vs 科创50 +0.29%）。
  行业迁移是**跨赛道搬家**：电网设备 6→1、半导体 5→2 几乎清空，医药链集体爆发（化学制药 0→8、医疗服务 2→5、医疗器械 0→4、中药 0→2，合计 24 家占 23.8%）+ 地产链补涨。
  晋级率 27.3%（连板组 66.7% / 中位 +9.96%，首板组 20.0% / 中位 +1.40%）——高标锁仓、首板快换手。
  封单合计 81.4 亿但新华传媒一只占 24.89 亿（30.6%），中位数反而从 0.52 亿降到 0.42 亿。

## 口径发现（本次新增）

- **东财 `em_ZT.tc` = 103 vs 同花顺 101**，差额 2 只均为北交所（920478 峆一药业、920427 华维设计）。
  报告口径说明已显式写明「按沪深口径取 101」——连续两日复查确认北交所标的会混入东财涨停池，建议把这条固化进 skill 自检项。
- **同花顺跌停池明细接口当日 404**（`limit_down_pool` 返回 404），`ths_dt` 为 None；跌停家数只能用汇总字段 `limit_down_count`。
  报告已声明「跌停 2 家（汇总口径），明细池不可用故未列名单」。**这是 skill 里「跌停家数用汇总字段」这条铁律的又一次实证。**
- `zt_fetch.resolve_dates` **只取前 2 个参数**，所以 `zt_review_{D0}.json` 只含 D0/D1 两天。
  但 `zt_analyze.py` 传 D2 时仍能出正确的三日序列——因为 `days[0]` 用的是 **`senti[D1]["zt_prev"]`**（即 D1 记录里带的「前一交易日」汇总值），不是 D2 自身的 senti。
  排查时一度以为 D2 数据缺失，实为设计如此，不必补抓。

## MEMORY.md 整理

- 项目 MEMORY.md 已超注入上限（13.7KB 被截断），本次做了一次全量合并重写（→ 约 9KB）：
  压缩数据源表格、合并同类工程约定、删去与 skill 重复的细节，保留全部可复用结论（红利季节性、培育钻石标本、自建等权指数坑、涨停周报口径）。

## git

- commit：`reports/涨停复盘对比-20260921.html` + `scripts/zt_report_20260921.py` + 记忆文件。
- `scratch_fund.txt`（gb2312 的 404 页面残留）仍忽略，未提交。

## 同日重复触发复跑（16:10，同一交易日第二次）

- 触发原因：同一自动化任务在同一交易日再次触发。**先查不是先重做** —— 发现报告已于 15:38 生成、commit `f6d1323` 已 push。
- **幂等验证**：重新 `zt_fetch.py 20260921 20260918` 后，按 `code` 排序归一化比对两份 JSON。
  结论：家数（101/77）、涨跌幅、封板时间、成交额**全部不变**；重跑 analyze + report 后
  新 HTML 与已提交版本**逐字节一致（仅生成时间一行不同）**（`git diff --stat` = 1 insertion / 1 deletion）→ 直接复用，未产生新提交。
- **新口径发现（已写入 skill pitfalls 第 10 条）**：同花顺封单 `order_amount` / `order_volume`
  在**收盘后 30 分钟内仍在刷新**——15:31 与 16:11 两次抓取，4 只个股差 18~54 倍
  （我爱我家 327万→5916万、百通能源 220万→1.198亿、华森制药 106万→3550万、奥赛康 13.3万→271万）。
  报告统一取**东财 `fund`**，故结论未受影响；但若改口径用同花顺封单，**必须 16:00 后再抓**。
- **比对方法教训**：同一份数据两次抓取的**列表顺序本身不稳定**（`em_ZB` / `ths_block` 首项互换），
  直接 diff 会看到几十处假差异 → 必须先按 `code` 排序归一化。代码片段已进 pitfalls 第 10 条。
- skill 更新：`SKILL.md` 必做自检新增「同交易日重复触发先做幂等比对」；`references/pitfalls.md` 新增口径类第 10 条。
- 工作区收尾：将重新生成的 HTML 还原为已提交版本（内容一致），删除临时备份 `out/_bak_zt_review_20260921.json`，工作区保持干净。

## 同日重复触发复跑（16:33，同一交易日第三次）

- 同样**先查后做**：报告与 commit 均已存在 → 幂等验证，未重做报告。
- **新坑（已写入 skill pitfalls 第 11 条）**：`index_hist` 的指数日线在盘后会**把当日行移出返回窗口**。
  09-21 实测：16:11 抓取含 `20260921`，16:34 与 16:36 两次抓取的末行均为 `20260918`（当日消失且稳定不复现）。
  属接口侧行为。
- **影响面判定**：**不影响报告** —— 当日成交额取自实时快照 `indexes[sym].amount_wan`，
  历史行按显式日期查 D1/D2（`hist_row(hcode, D1)`），脚本对窗口滑动免疫。
  但如果有人图省事用 `rows[-1]` 当「当日」，会静默回退一天且不报错 → 已在 skill 中明令禁止。
- **比对方法升级**：幂等比对**只比 `D0`/`D1`/`today_zt_quotes`/`yesterday_zt_today`** 四个核心键；
  必须排除 `generated_at`、`dates`、`indexes`（市值尾数抖动）、`index_hist`（本条窗口滑动）。
  本次三次抓取四个核心键全部一致 → 报告可复用。若把 `index_hist` 纳入比对会看到 8 行假差异而误判。
- 收尾：`out/zt_review_20260921.json` 已从 16:11 备份还原（含当日日线，与已提交报告同源）。
